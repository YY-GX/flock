# bird_web — TODO

状态截至 2026-09-21 清晨。**六个视图、397 个页面**。A 组（主人提的四条）、
B 组（美工十一条）、C 组（非美工八条）以及 `DESIGN2.md` 的 I1/I2/I8 已全部落地。
本文件是所有 agent 的共享参照。`AUDIT.md` 是第一轮审计原始报告（已全部处理），
`DESIGN.md` / `DESIGN2.md` 是两轮美术指导稿。

## 已完成

| 组 | 内容 |
|---|---|
| **A1** | 鸟图放大 + 拖拽（点击/双击/捏合/滚轮，锚定指针，上限锁死原图像素，翻页重置） |
| **A2** | RTP 手绘墨圈 → 箭头 → 虚线 inset 框；箭头 32.7 → **71.5 单位**，修了绘制顺序把箭头洗白的 bug |
| **A3** | 代表鸟去重：37 个 pin **37 张不同照片**（原来红雀占 10 个）+ 悬停翻页（154 张卡，静止零加载） |
| **A4** | 首页变形可读性：鸟群底下淡入铅笔美国海岸线 / 时间轴骨架 + 一行说明；墨圈现在只圈住 34 只**真·本地鸟** |
| **B1–B4** | 纸纹（334B，底色预补偿）· 暖卡纸 + 斜切高光 · 空心框压印 · 自托管 Bodoni Moda（8.5KB，六款比稿） |
| **B5** | 只做了时间轴季节分界线；空心框和标题下划线**有理由地拒绝了** |
| **B6** | 首页景深（外圈模糊 + 降饱和），241 帧基线零损失 |
| **B7** | 手绘指北针（角度从 Albers 投影反算 **−0.23°**）+ 比例尺（500 mi = 166.84 单位）；图例放不下，改成地图下方文字列表 |
| **B8** | 中文名竖排题记（新 `--serif-cjk`）；字号从设计稿的 13.8px 改成 15.2px，因为宋体横画在小字号下对比度掉到 3.78 |
| **B9** | 季节色跟随真实日期（九月琥珀）；地面从不着色（夏季色仅 2.11:1） |
| **B10** | **397 张 OG 卡片** + 完整 meta + favicon 全套（上一版是个「煎蛋」，已重调） |
| **B11** | 打印样式：`/list` 16 页接触印相表 + 打勾清单，`/birds/<slug>` 一页标本记录 |
| **C1** | `/list` build 时真裁剪：清晰度 **2.44×**、不再放大；代价 +35% 流量（原「省 40%」是错的） |
| **C2** | 时间轴点重叠：最近邻 2.5px → **14.38px**，圆心被盖 190/238 → **0/238**（点阵间距 22 > 最大命中半径 21，构造保证） |
| **C3** | 手机地点索引（不是聚合）：地图原样不动，360px 下地图**下方**印出 37 行地点表（每行 48px，全部 ≥24px）；另加不可见 thumb pad，把 pin 命中区撑到 24px 或邻居间隙的一半（900px 下最小 pin 23.45 → **24.00**，桌面 1280/1440 完全不变） |
| **C4** | 穿过标题的鸟：Where 13→5、When 19→7；水平弦数学上弯不开，到不了零 |
| **C5** | 跨地图：文字行修好（位移 75px → **0px**）+ 298 个 tile 加了 `↗` 通往鸟页 |
| **C6** | 时间轴 **10 条旅行括号**，可点进 `/days/<iso>` |
| **C7** | 交互差异保留但**点之前看得出来**：链接 `cursor: pointer` + 悬停下划线，浮层 `cursor: zoom-in` |
| **C8** | ARIA：`role="img"` 去掉、脚手架 `aria-hidden`（静态文本 1587 → 44）、修了 `link > link` 嵌套 |
| **I1** | **`/days`** —— 64 天野外笔记本流水账 + 64 个日期页 |
| **I2** | **四条铅笔旅行路线**（数据只支持这么多；夏威夷和西雅图是单地点，画不出线） |
| **I8** | 浮层「That day」同伴行；诚实数字是 **219 有 / 19 没有**（设计稿的 216 暗含了「同地点」） |
| **I7** | `/list` 空心框之后的版本记（colophon）：**1,278 张**照片 = X-T5 1,054 + A7C II 151 + iPhone 73（数字从 `photos.json` 现算）。六条相机原始写法在 `listView.ts` 里统一（`XT-5`→`X-T5`、`iphone`→`iPhone`），鸟页那行也跟着修正。镜头拆分（427/627）**故意不印**；打印仍是 16 页，版本记落在最后一页打勾框下面 |
| **B9 尾巴** | 首页 `.way::after` 改 `var(--accent)`（第 9 个也是最后一个槽）。实测像素对比度：秋 3.86 / 冬 3.84 / 春 3.10 / 夏 2.15（深色 6.83–10.06）——都是「标记不是文字」，1px 不用退到 2px |

## 仍未完成

| # | 项目 | 卡在哪 |
|---|---|---|
| I3–I6, I9 | `DESIGN.md` §4 剩余点子（I7 已完成，见上表） | 进行中（由 agent 自行取舍） |
| — | `astro.config.mjs` 加 `site: 'https://…'` | **需要域名**，有了之后所有分享链接自动转绝对路径 |

---

## D. 需要主人在 Notion 里做的（agent 不要动）

- **4 组重复卡片**：Greater Yellowlegs / Brown Creeper / Swainson's Thrush /
  Pied-billed Grebe —— 每组一张有观测记录、一张有全部资料，两页都半空
- **Fish Crow** spotted 但无照片
- **Northern Rough-winged Swallow** 标了未 spotted 却有日期/类型/地点
- ~~**Great Blue Heron** 照片页图太多，拆成两页~~ —— **不用拆，已解决**。
  29 张图已全部抓下来（`photos.json` 从 1,278 → **1,307** 张）。原因见
  CLAUDE.md：工具返回超限 ≠ 数据拿不到，完整返回在报错前就落盘了
- **Great Blue Heron 的 `Camera` 填的是 "iphone 15 Pro Max"，但对不上**：
  29 张里 EXIF 读出来 5 张是富士（7728×5152，X-T5）、2 张是 iPhone、
  其余 22 张是没有相机信息的截图/裁切。`Camera` 是每页一个值，这页混了机器，
  所以 `/list` 版本记里 iPhone 那一栏现在多算了至少 5 张。
  要么在 Notion 里拆页，要么改成按文件读 EXIF——**没有替你猜**
- **`focal.json`** 可能有几条算错，翻到明显不对的手动改并设 `"by": "hand"`

---

## 关键约束（所有 agent 必读）

- `src/lib/README.md` 是组件契约，先读
- **`prune-originals.mjs` 靠扫描产物文本里的字面文件名决定保留**。
  改动图片路径或任何内联编码后，必须验证 `dist/` 里每张图都还在——
  曾经有过 build 绿但 1042 张图被静默删除
- `--ink-faint` 4.88:1 / 5.45:1、罗盘 3.23:1 / 4.25:1，**不许回退**
- 43 个空心框、统一 3:4 网格、query-string 筛选状态、~1.3ms 筛选，不许回退
- dev server 已在 8888 跑，**不要启动或杀掉**；自己的 Chrome 用别的端口
- clean build 后要重新 `xattr -w com.dropbox.ignored 1 dist`
- Dropbox 卷上 Vite 有时给过期资源，`touch` 一下
- 多个 agent **不要同时 `npm run build`**，会互相破坏
